/**
 * FTC dependent classes and implementations that are reusable. Does not include
 * control maps or game-specific hardware configurations, but can include templates
 * for common subsystems. Some code, like implementations of interfaces in the
 * {@link org.westtorrancerobotics.lib.hardware} package over the FTC hardware classes,
 * and a drive train template, as well as any wrappers or utilities to improve
 * efficiency or potential for utility within the FTC library, should be contained
 * here.
 * 
 * @since 1.1
 */
package org.westtorrancerobotics.lib.ftc;
